import { PatientImageReport } from './patient-image-report';

describe('PatientImageReport', () => {
  it('should create an instance', () => {
    expect(new PatientImageReport()).toBeTruthy();
  });
});
